<div class="header_avatar">
    {{#if avatar}}
        <img src={{avatar}} alt="avatar" />
    {{/if}}
</div>
<div class="header_name">
    <span>{{title}}</span>
</div>
<div class="header_menu">
    {{{menuButton}}}
</div>
{{{menu}}}
{{{overlay}}}
